'use strict';

/**
 * verbete-instituicao router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::verbete-instituicao.verbete-instituicao');
