'use strict';

/**
 * verbete-instituicao service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::verbete-instituicao.verbete-instituicao');
